import React from 'react';
import './App.css';
import { Formik, Form, Field, ErrorMessage } from 'formik';

const RegistrationForm = () => ( {errors, touched, values, handleSubmit, status}
  const [user, setUser] = useState([]);
  console.log(user);
  <div>
    <h1>REGISTER HERE</h1>
  <Formik>


)

export default RegistrationForm
